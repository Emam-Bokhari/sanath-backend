export enum USER_ROLES {
  ADMIN = "ADMIN",
  SUPER_ADMIN = "SUPER_ADMIN",
  USER = "USER",
  AGENT = "AGENT",
}

export enum GENDER {
  MALE = "MALE",
  FEMALE = "FEMALE",
}

export enum STATUS {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}
